version: "2"

sql:
  - engine: "postgresql"
    schema: "migrations/*.sql"
    queries: "queries/users.sql"
    gen:
      go:
        out: "../../generated/database/users"
        omit_unused_structs: true
